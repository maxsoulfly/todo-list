import { setupAddProjectButton, renderProjects } from "./ui/projectUI.js";
import { renderTasks } from "./ui/taskUI.js";
import { renderSidebar, setupSidebarToggle } from "./ui/sidebarUI.js";

export {
    renderProjects,
    renderTasks,
    setupAddProjectButton,
    renderSidebar,
    setupSidebarToggle,
};
